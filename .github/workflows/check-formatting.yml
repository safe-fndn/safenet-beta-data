name: Check Formatting

on:
  pull_request:
  push:

jobs:
  formatting:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v6

      - run: |
          make fix
          git diff --exit-code
